stages:
  - build
  - test
  - deploy

image: python:latest

before_script:
  - pip install -r requirements.txt

run_api:
  stage: build
  script:
    - python main.py &
    - sleep 20
    - curl http://localhost/api/health | grep "UP"